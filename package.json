{
  "name": "dc-robin",
  "displayName": "DC Robin",
  "description": "workflow assistant",
  "publisher": "ZenofSoftware",
  "icon": "icon.png",
  "version": "0.0.5",
  "repository": {
    "type": "git",
    "url": "https://github.com/cyx-1/dc-robin.git"
  },
  "engines": {
    "vscode": "^1.93.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "webviewHello.showHello",
        "title": "Show Hello"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "webviewHello",
          "title": "Hello",
          "icon": "resources/hello-icon.svg"
        }
      ]
    },
    "views": {
      "webviewHello": [
        {
          "id": "webviewHello.view",
          "name": "Hello View"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.7",
    "@types/node": "20.x",
    "@types/vscode": "^1.93.0",
    "@typescript-eslint/eslint-plugin": "^8.3.0",
    "@typescript-eslint/parser": "^8.3.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "esbuild": "^0.23.1",
    "eslint": "^9.9.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "dc-robin": "file:"
  }
}